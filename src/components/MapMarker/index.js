import React from 'react';

const MapMarker = (props) => (
  <div style={{
    height: '24px',
    width: '24px',
    marginTop: '-30px',
    backgroundImage: 'url("/marker.png")',
    backgroundSize: '100%',
  }}>
  </div>
);

export default MapMarker;
